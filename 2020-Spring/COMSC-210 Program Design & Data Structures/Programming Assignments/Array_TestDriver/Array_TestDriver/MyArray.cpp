#include <iostream>
#include <cstdlib>

#include "Array.h"

using namespace std;

int main() {
  
  Array a;

  string buf;
  int index, value = 0;

  while (1) {
    cin >> buf;
    if(buf[0] == 'q' || buf[0] == 'Q') break;//Break if input is 0
    index = atoi(buf.c_str());

  }

}